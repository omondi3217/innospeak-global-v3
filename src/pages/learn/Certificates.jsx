import { useEffect, useState } from 'react';
import { Award, ExternalLink, ShieldCheck } from 'lucide-react';
import Seo from '../../components/ui/Seo.jsx';
import SectionCard from '../../components/portal/SectionCard.jsx';
import { listMyCertificates } from '../../lib/supabase/learning.js';
export default function Certificates(){const[data,setData]=useState([]);const[error,setError]=useState('');useEffect(()=>{listMyCertificates().then(setData).catch(e=>setError(e.message))},[]);return <><Seo title="My Certificates" description="Your verified InnoSpeak learning credentials." path="/learn/certificates"/><div><p className="eyebrow">Credentials</p><h1 className="page-title">My Certificates</h1><p className="page-copy">Completed courses can issue verifiable learning credentials.</p></div>{error&&<div className="mt-5 rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</div>}<div className="mt-6 grid gap-4 md:grid-cols-2">{!data.length?<SectionCard title="No certificates yet"><p className="text-sm text-navy-500">Complete an eligible course to receive a certificate.</p></SectionCard>:data.map(c=><SectionCard key={c.id} className="p-6"><div className="flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-gold-500/10 text-gold-700"><Award size={20}/></div><div><h2 className="font-display font-bold text-navy-900">{c.lms_courses?.title||'Course certificate'}</h2><p className="text-xs text-navy-500">{c.certificate_number}</p></div></div><p className="mt-4 text-sm text-navy-600"><ShieldCheck className="mr-1 inline text-emerald-600" size={15}/> Issued {new Date(c.issued_at).toLocaleDateString()}</p><a href={`/verify-certificate?number=${encodeURIComponent(c.certificate_number)}`} className="mt-4 inline-flex items-center gap-2 font-body text-sm font-bold text-gold-700">Verify credential <ExternalLink size={14}/></a></SectionCard>)}</div></>}
